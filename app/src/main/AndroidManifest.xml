<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.nirushan.carddetails">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CardDetails">
        <activity
            android:name="org.nirushan.carddetails.AddUpdatePayment"
            android:exported="true" />
        <activity
            android:name="org.nirushan.carddetails.PaymentView"
            android:exported="true" />
        <activity
            android:name="org.nirushan.carddetails.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CardDetails.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>